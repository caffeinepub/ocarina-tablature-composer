{
  "frontend/src/App.tsx": "Main app with TanStack Router, sidebar navigation, auth gate (login + registration), and all page components; includes SettingsPage with SoundSettingsPanel supporting preset loading, DesignEditorPage with OcarinaDesignEditor, and proper routing for all pages.",
  "frontend/src/pages/HomePage.tsx": "Home/landing page with hero section, fingering chart preview using OcarinaVisual with `holeStates` prop, and feature cards linking to all app sections.",
  "frontend/src/components/SongEditor.tsx": "Full song editor with piano keyboard (C5–C6), tablature display using TablatureSymbol, ocarina-style audio synthesis (sine/triangle blend with reverb envelope) for both live play and playback, save functionality, and preset song loading from router state.",
  "frontend/src/hooks/useQueries.ts": "React Query hooks for all backend operations; restores OCTAVE_RANGES as `{label, value}[]` objects and getDefaultFingeringPattern with an optional noteName parameter returning per-note 4-hole boolean patterns.",
  "frontend/src/pages/PresetSongsPage.tsx": "Preset songs page grouped by genre (Folk, Rock, Electronic, Reggaeton, Reggae) with song cards; fixed TanStack Router navigate type error by using `window.history.replaceState` to pass the preset song state before navigating.",
  "frontend/src/components/SoundSettingsPanel.tsx": "Sound settings panel with tone (character/warmth), reverb (mix/amount/delay), and pitch offset sliders that update the backend in real-time, plus a Sound Presets section for saving named presets with optional ocarina image and loading/displaying saved presets.",
  "frontend/src/components/OcarinaVisual.tsx": "SVG ocarina visual with exactly 4 holes in a 2×2 grid; accepts both `holeStates` and `fingeringPattern` prop names for backward compatibility, plus optional custom image URL, interactive clicking, size variants, and showLabels support.",
  "frontend/src/components/TablatureSymbol.tsx": "SVG tablature symbol rendering 4 holes in a 2×2 grid; accepts both `fingeringPattern` and legacy `pattern` prop names, supports `noteName`/`noteOctave` labels, `customLayout`, and `size` as either named variant ('sm'/'md'/'lg') or legacy numeric pixel width.",
  "frontend/src/components/OcarinaDesignEditor.tsx": "Canvas-based ocarina design editor with draggable 4-hole 2×2 layout, background image upload, hole size adjustment, save to backend, and support for loading a preset image URL via the presetImageUrl prop.",
  "frontend/src/components/StaffNotation.tsx": "Staff notation SVG component showing a treble clef note head with stem and 5 staff lines; fixed to pass `size=\"sm\"` (string) instead of `size={32}` (number) to TablatureSymbol."
}